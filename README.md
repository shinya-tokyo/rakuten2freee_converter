# rakuten2freee_converter
楽天証券の有価証券取引報告書をfreee会計で読み込めるようにするものです。

# 使い方

1．次を、実行する。

python rakuten_to_free.py

2. 次が現れたら、読み込むCSVファイルの名前(とパス)を入力する。

input_file >>

3. 次に、出力するCSVファイルの名前(とパス)を入力する。

4. プレビューがコマンドライン上に出る。

5. 3のファイルが出力される。

# 入力例


